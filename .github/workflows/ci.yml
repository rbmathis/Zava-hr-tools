name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: echo "Building Zava-hr-tools..."
      - name: Test
        run: echo "Testing Zava-hr-tools... All tests passed."
